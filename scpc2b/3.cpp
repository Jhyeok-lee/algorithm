#include <iostream>
#include <cstdio>
#include <cstring>

using namespace std;

int N;
char s[5001];

int main(void)
{
	setbuf(stdout, NULL);
	int Case;
	scanf("%d", &Case);
	for(int test=0; test<Case; test++) {
		scanf("%d %s", &N, s);
		
		printf("Case #%d\n%d\n", test+1, ret);
	}
	return 0;
}